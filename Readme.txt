In order to run SteamRoller scripts you will need to unblock them in PowerShell.

Start PowerShell in Administrator mode and execute the following command:

  Unblock-File -Path "<apppath>\SteamMode.ps1"
 